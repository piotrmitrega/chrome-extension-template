export enum ContentScriptCommandType {
  ENABLE_PICK_MODE = "enable-pick-mode",
  DISABLE_PICK_MODE = "disable-pick-mode",
}

export enum PopupCommandType {
  PICK_MODE_COMPLETED = "pick-mode-completed",
}
