export enum ContentCommandType {
  ENABLE_SELECTION_MODE = "enable-selection-mode",
  DISABLE_SELECTION_MODE = "disable-selection-mode",
}

export enum PopupCommandType {
  NOTHING = "nothing",
}

export enum BackgroundCommandType {
  SAVE_PRODUCT_DATA_SELECTOR = "save-product-data-selector",
}
